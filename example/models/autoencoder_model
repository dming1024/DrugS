
becauset the 'autoencoder_model.h5' is too big to upload, so you have to refere to the notebook.ipynb to build your own `autoencoder_model.h5` and place it here to make the model work.

or ask me (dongliulou@126.com) for `autoencoder_model.h5`.